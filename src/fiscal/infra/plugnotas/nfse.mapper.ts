import {
  extractDocumentIdentifiers as extractPlugNotasDocumentIdentifiers,
  extractProviderStatus as extractPlugNotasStatus,
  mapProviderStatusToDomain as mapPlugNotasStatusToDomain,
  GenericDocumentParser,
} from '../../domain/generic-document-parser';
import { PlugNotasDocumentParser } from './plugnotas-document-parser';

export {
  extractPlugNotasDocumentIdentifiers,
  extractPlugNotasStatus,
  mapPlugNotasStatusToDomain,
  GenericDocumentParser,
  PlugNotasDocumentParser,
};
